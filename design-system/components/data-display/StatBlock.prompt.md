Big bold number with a short label underneath — used in proof-point rows ("400+ projects", "18 years"). Set `tone="dark"` on a dark/ink background.

```jsx
<StatBlock value="18" label="Years in business" />
```
