Small uppercase label for project categories, status flags or alerts (e.g. "New build", "In progress").

```jsx
<Tag tone="yellow">Renovation</Tag>
<Tag tone="red">Sold out</Tag>
```

Tones: `ink` (default), `yellow`, `red`, `pink`, `outline`.
